# Fixtures

Keep small, deterministic raw inputs and canonical JSON examples here. Stage 1 does not parse fixtures; later ingestion tests should use this directory instead of RimWorld or DevBridge2 at test time.

`stage7-grouped.log` is a multi-symptom initialization/tick/draw fixture with one independent save failure.
